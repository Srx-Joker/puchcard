# 打卡网站
## 后端
```shell
# 安装依赖
cd backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# 运行后端
cd ..
python -m backend.main
```

## 前端
```shell
# 安装依赖
cd frontend
npm install

# 运行前端
npm run start
```